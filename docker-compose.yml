version: "3.9"

services:
  tts-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tts-api
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
